# 计算机视觉小鼠管理系统开发里程碑规划

## 项目概述

**开发团队规模：**
- 第1个月：1人开发（您）
- 第2个月开始：2人开发团队
- 数据标注：第2个月开始

**开发策略：**
- 优先开发Spring Boot后端项目
- 集成二维码管理功能
- 使用假数据进行开发和测试
- 采用敏捷开发模式，快速迭代

## 第一阶段：基础架构搭建（第1-2周）

### 目标
建立项目基础架构，完成核心框架搭建

### 主要任务

#### 1. 项目初始化
- [ ] 基于RuoYi-Vue-Plus创建项目骨架
- [ ] 配置开发环境（Docker环境）
- [ ] 设置代码规范和Git工作流
- [ ] 配置CI/CD基础流程

#### 2. 数据库设计与实现
- [ ] 完善数据库表结构设计
- [ ] 编写数据库初始化脚本
- [ ] 配置数据库连接和事务管理
- [ ] 实现数据库版本管理（Flyway）

#### 3. 基础模块开发
- [ ] 用户管理模块（基于RuoYi）
- [ ] 权限管理模块
- [ ] 系统配置模块
- [ ] 日志管理模块
- [ ] 二维码生成和管理模块（小鼠、鼠笼、设备标识）

### 交付物
- 可运行的Spring Boot项目
- 完整的数据库结构
- 基础的用户认证和权限管理
- 开发环境Docker配置

## 第二阶段：核心业务模块开发（第3-4周）

### 目标
实现核心业务功能，建立完整的API体系

### 主要任务

#### 1. 设备管理模块
- [ ] 设备注册和认证
- [ ] 设备状态监控
- [ ] 设备配置管理
- [ ] 设备心跳机制

#### 2. 实验管理模块
- [ ] 实验项目管理
- [ ] 鼠笼管理
- [ ] 小鼠信息管理
- [ ] 实验计划制定
- [ ] 二维码标签管理（小鼠、鼠笼标识）

#### 3. 数据接收模块
- [ ] 行为数据接收API
- [ ] 数据验证和清洗
- [ ] 批量数据处理
- [ ] 数据存储优化

### 交付物
- 完整的设备管理功能
- 实验和小鼠管理功能
- 数据接收和存储功能
- API文档（APIFOX）

## 第三阶段：数据分析与展示（第5-6周）

### 目标
实现数据分析和可视化功能

### 主要任务

#### 1. 数据分析模块
- [ ] 行为统计分析
- [ ] 趋势分析算法
- [ ] 异常检测机制
- [ ] 报表生成功能

#### 2. 前端开发
- [ ] 管理后台界面
- [ ] 数据可视化图表
- [ ] 实时监控界面
- [ ] 移动端适配准备
- [ ] 二维码扫描和管理界面

#### 3. 实时通信
- [ ] WebSocket实时推送
- [ ] 消息队列集成
- [ ] 实时告警机制

### 交付物
- 数据分析和统计功能
- 完整的Web前端界面
- 实时数据推送功能

## 第四阶段：系统集成与优化（第7-8周）

### 目标
系统集成测试，性能优化，准备生产部署

### 主要任务

#### 1. 系统集成
- [ ] Python采集端模拟器开发
- [ ] 端到端测试
- [ ] 性能压力测试
- [ ] 安全性测试

#### 2. 数据标注准备
- [ ] 标注工具集成（CVAT）
- [ ] 标注任务管理
- [ ] 标注质量控制
- [ ] 标注数据导出

#### 3. 部署优化
- [ ] Docker镜像优化
- [ ] 生产环境配置
- [ ] 监控和日志系统
- [ ] 备份和恢复机制

### 交付物
- 完整的系统集成测试
- 生产就绪的Docker镜像
- 标注工具和流程
- 部署和运维文档

## Spring Boot项目Model划分

### 1. 核心实体模型

#### 1.1 用户管理模块
```java
// 用户实体
@Entity
@Table(name = "t_user")
public class User extends BaseEntity {
    private Long userId;
    private String username;
    private String password;
    private String email;
    private String phone;
    private String realName;
    private String avatar;
    private Integer status;
    private String roles;
    private Date lastLoginTime;
    // getter/setter...
}

// 角色实体
@Entity
@Table(name = "t_role")
public class Role extends BaseEntity {
    private Long roleId;
    private String roleName;
    private String roleKey;
    private String description;
    private Integer status;
    private List<Permission> permissions;
    // getter/setter...
}
```

#### 1.2 二维码管理模块
```java
// 二维码实体
@Entity
@Table(name = "t_qr_code")
public class QrCode extends BaseEntity {
    private Long qrCodeId;
    private String qrCodeData; // 二维码数据内容
    private String qrCodeType; // mouse, cage, device, experiment
    private String targetId; // 关联的目标ID（小鼠ID、鼠笼ID等）
    private byte[] qrCodeImage; // 二维码图片
    private String imageUrl; // 图片存储URL
    private Integer status; // 0-无效 1-有效
    private String description;
    // getter/setter...
}

// 二维码扫描记录
@Entity
@Table(name = "t_qr_scan_log")
public class QrScanLog extends BaseEntity {
    private Long scanId;
    private String qrCodeData;
    private String scanUserId;
    private String deviceInfo;
    private String location;
    private Date scanTime;
    private String scanResult;
    // getter/setter...
}
```

#### 1.3 设备管理模块
```java
// 设备实体
@Entity
@Table(name = "t_device")
public class Device extends BaseEntity {
    private String deviceId;
    private String deviceName;
    private String deviceType;
    private String location;
    private String macAddress;
    private String ipAddress;
    private Integer status;
    private String capabilities;
    private String hardwareInfo;
    private String configInfo;
    private Date lastHeartbeat;
    private String deviceToken;
    private String apiKey;
    // getter/setter...
}

// 设备状态实体
@Entity
@Table(name = "t_device_status")
public class DeviceStatus extends BaseEntity {
    private Long statusId;
    private String deviceId;
    private Date timestamp;
    private String status;
    private Double cpuUsage;
    private Double memoryUsage;
    private Double gpuUsage;
    private Double diskUsage;
    private Double temperature;
    private Double fps;
    private Integer detectionCount;
    private Double trackingAccuracy;
    // getter/setter...
}
```

#### 1.4 实验管理模块
```java
// 实验项目实体
@Entity
@Table(name = "t_experiment")
public class Experiment extends BaseEntity {
    private String experimentId;
    private String experimentName;
    private String description;
    private String researcherName;
    private String institution;
    private Date startTime;
    private Date endTime;
    private Integer status;
    private String objectives;
    private String methodology;
    private String expectedResults;
    // getter/setter...
}

// 鼠笼实体
@Entity
@Table(name = "t_cage")
public class Cage extends BaseEntity {
    private String cageId;
    private String cageName;
    private String experimentId;
    private String deviceId;
    private String cageType;
    private String environment;
    private Integer capacity;
    private Integer currentCount;
    private String location;
    private String description;
    // getter/setter...
}

// 小鼠实体
@Entity
@Table(name = "t_mouse")
public class Mouse extends BaseEntity {
    private String mouseId;
    private String mouseName;
    private String cageId;
    private String experimentId;
    private String strain;
    private String gender;
    private Integer age;
    private Double weight;
    private String healthStatus;
    private String geneticBackground;
    private String treatmentGroup;
    private Date birthDate;
    private String description;
    // getter/setter...
}
```

#### 1.5 行为数据模块
```java
// 行为数据实体
@Entity
@Table(name = "t_behavior_data")
public class BehaviorData extends BaseEntity {
    private Long dataId;
    private String mouseId;
    private String deviceId;
    private String cageId;
    private Date timestamp;
    private Double positionX;
    private Double positionY;
    private Double positionConfidence;
    private String behaviorType;
    private Double behaviorConfidence;
    private Integer behaviorDuration;
    private Double velocity;
    private Double acceleration;
    private Double direction;
    private String environmentalData;
    private String rawData;
    // getter/setter...
}

// 行为统计实体
@Entity
@Table(name = "t_behavior_statistics")
public class BehaviorStatistics extends BaseEntity {
    private Long statisticsId;
    private String mouseId;
    private String experimentId;
    private Date statisticsDate;
    private String behaviorType;
    private Integer totalCount;
    private Long totalDuration;
    private Double averageDuration;
    private Double frequency;
    private String timeDistribution;
    private String additionalMetrics;
    // getter/setter...
}
```

#### 1.6 标注管理模块
```java
// 标注任务实体
@Entity
@Table(name = "t_annotation_task")
public class AnnotationTask extends BaseEntity {
    private String taskId;
    private String taskName;
    private String experimentId;
    private String assigneeId;
    private String taskType;
    private Integer status;
    private String videoPath;
    private Date startTime;
    private Date endTime;
    private String instructions;
    private Integer priority;
    private Double progress;
    // getter/setter...
}

// 标注结果实体
@Entity
@Table(name = "t_annotation_result")
public class AnnotationResult extends BaseEntity {
    private Long resultId;
    private String taskId;
    private String annotatorId;
    private Date timestamp;
    private String mouseId;
    private String behaviorType;
    private Double confidence;
    private String boundingBox;
    private String keyPoints;
    private String notes;
    private Integer quality;
    // getter/setter...
}
```

### 2. 核心接口定义

#### 2.1 二维码管理接口
```java
@RestController
@RequestMapping("/api/qrcode")
@Api(tags = "二维码管理")
public class QrCodeController {
    
    @PostMapping("/generate")
    @ApiOperation("生成二维码")
    public R<QrCodeGenerateResponse> generateQrCode(@RequestBody QrCodeGenerateRequest request) {
        // 二维码生成逻辑
    }
    
    @GetMapping("/scan/{qrCodeData}")
    @ApiOperation("扫描二维码")
    public R<QrCodeScanResponse> scanQrCode(@PathVariable String qrCodeData) {
        // 扫描处理逻辑
    }
    
    @GetMapping("/info/{targetId}")
    @ApiOperation("获取目标二维码信息")
    public R<QrCode> getQrCodeByTarget(@PathVariable String targetId, @RequestParam String targetType) {
        // 获取二维码信息
    }
    
    @GetMapping("/batch/generate")
    @ApiOperation("批量生成二维码")
    public R<BatchQrCodeResponse> batchGenerateQrCode(@RequestBody BatchQrCodeRequest request) {
        // 批量生成逻辑
    }
    
    @GetMapping("/scan/history")
    @ApiOperation("扫描历史记录")
    public TableDataInfo<QrScanLog> getScanHistory(QrScanQueryRequest request) {
        // 扫描历史查询
    }
}
```

#### 2.2 设备管理接口
```java
@RestController
@RequestMapping("/api/device")
@Api(tags = "设备管理")
public class DeviceController {
    
    @PostMapping("/register")
    @ApiOperation("设备注册")
    public R<DeviceRegisterResponse> registerDevice(@RequestBody DeviceRegisterRequest request) {
        // 设备注册逻辑
    }
    
    @PostMapping("/heartbeat")
    @ApiOperation("设备心跳")
    public R<Void> heartbeat(@RequestBody DeviceHeartbeatRequest request) {
        // 心跳处理逻辑
    }
    
    @GetMapping("/config/{deviceId}")
    @ApiOperation("获取设备配置")
    public R<DeviceConfig> getDeviceConfig(@PathVariable String deviceId) {
        // 获取配置逻辑
    }
    
    @GetMapping("/list")
    @ApiOperation("设备列表")
    public TableDataInfo<Device> getDeviceList(DeviceQueryRequest request) {
        // 设备列表查询
    }
    
    @PutMapping("/status/{deviceId}")
    @ApiOperation("更新设备状态")
    public R<Void> updateDeviceStatus(@PathVariable String deviceId, @RequestBody DeviceStatusRequest request) {
        // 状态更新逻辑
    }
}
```

#### 2.3 行为数据接口
```java
@RestController
@RequestMapping("/api/behavior")
@Api(tags = "行为数据管理")
public class BehaviorController {
    
    @PostMapping("/batch")
    @ApiOperation("批量上报行为数据")
    public R<BatchUploadResponse> uploadBehaviorData(@RequestBody BatchUploadRequest request) {
        // 批量数据处理
    }
    
    @GetMapping("/statistics")
    @ApiOperation("行为统计查询")
    public R<BehaviorStatisticsResponse> getBehaviorStatistics(BehaviorStatisticsRequest request) {
        // 统计数据查询
    }
    
    @GetMapping("/timeline/{mouseId}")
    @ApiOperation("小鼠行为时间线")
    public R<List<BehaviorTimelineItem>> getBehaviorTimeline(
            @PathVariable String mouseId,
            @RequestParam Date startTime,
            @RequestParam Date endTime) {
        // 时间线数据
    }
    
    @GetMapping("/realtime/{cageId}")
    @ApiOperation("实时行为数据")
    public R<List<RealtimeBehaviorData>> getRealtimeBehaviorData(@PathVariable String cageId) {
        // 实时数据
    }
}
```

#### 2.4 实验管理接口
```java
@RestController
@RequestMapping("/api/experiment")
@Api(tags = "实验管理")
public class ExperimentController {
    
    @PostMapping
    @ApiOperation("创建实验")
    public R<Experiment> createExperiment(@RequestBody CreateExperimentRequest request) {
        // 创建实验逻辑
    }
    
    @GetMapping("/list")
    @ApiOperation("实验列表")
    public TableDataInfo<Experiment> getExperimentList(ExperimentQueryRequest request) {
        // 实验列表查询
    }
    
    @GetMapping("/{experimentId}/mice")
    @ApiOperation("实验小鼠列表")
    public R<List<Mouse>> getExperimentMice(@PathVariable String experimentId) {
        // 实验小鼠查询
    }
    
    @PostMapping("/{experimentId}/mice")
    @ApiOperation("添加实验小鼠")
    public R<Void> addMouseToExperiment(@PathVariable String experimentId, @RequestBody AddMouseRequest request) {
        // 添加小鼠到实验
    }
    
    @GetMapping("/{experimentId}/report")
    @ApiOperation("生成实验报告")
    public R<ExperimentReport> generateExperimentReport(@PathVariable String experimentId) {
        // 生成报告
    }
}
```

#### 2.5 标注管理接口
```java
@RestController
@RequestMapping("/api/annotation")
@Api(tags = "标注管理")
public class AnnotationController {
    
    @PostMapping("/task")
    @ApiOperation("创建标注任务")
    public R<AnnotationTask> createAnnotationTask(@RequestBody CreateAnnotationTaskRequest request) {
        // 创建标注任务
    }
    
    @GetMapping("/task/list")
    @ApiOperation("标注任务列表")
    public TableDataInfo<AnnotationTask> getAnnotationTaskList(AnnotationTaskQueryRequest request) {
        // 任务列表查询
    }
    
    @PostMapping("/result")
    @ApiOperation("提交标注结果")
    public R<Void> submitAnnotationResult(@RequestBody SubmitAnnotationResultRequest request) {
        // 提交标注结果
    }
    
    @GetMapping("/quality/{taskId}")
    @ApiOperation("标注质量评估")
    public R<AnnotationQualityReport> getAnnotationQuality(@PathVariable String taskId) {
        // 质量评估
    }
}
```

### 3. 服务层设计

#### 3.1 核心服务接口
```java
// 二维码管理服务
public interface IQrCodeService {
    QrCodeGenerateResponse generateQrCode(QrCodeGenerateRequest request);
    QrCodeScanResponse scanQrCode(String qrCodeData);
    QrCode getQrCodeByTarget(String targetId, String targetType);
    BatchQrCodeResponse batchGenerateQrCode(BatchQrCodeRequest request);
    List<QrScanLog> getScanHistory(QrScanQueryRequest request);
}

// 设备管理服务
public interface IDeviceService {
    DeviceRegisterResponse registerDevice(DeviceRegisterRequest request);
    void processHeartbeat(DeviceHeartbeatRequest request);
    DeviceConfig getDeviceConfig(String deviceId);
    List<Device> getDeviceList(DeviceQueryRequest request);
    void updateDeviceStatus(String deviceId, DeviceStatusRequest request);
}

// 行为数据服务
public interface IBehaviorDataService {
    BatchUploadResponse processBehaviorData(BatchUploadRequest request);
    BehaviorStatisticsResponse getBehaviorStatistics(BehaviorStatisticsRequest request);
    List<BehaviorTimelineItem> getBehaviorTimeline(String mouseId, Date startTime, Date endTime);
    List<RealtimeBehaviorData> getRealtimeBehaviorData(String cageId);
}

// 实验管理服务
public interface IExperimentService {
    Experiment createExperiment(CreateExperimentRequest request);
    List<Experiment> getExperimentList(ExperimentQueryRequest request);
    List<Mouse> getExperimentMice(String experimentId);
    void addMouseToExperiment(String experimentId, AddMouseRequest request);
    ExperimentReport generateExperimentReport(String experimentId);
}
```

### 4. 假数据生成策略

#### 4.1 数据初始化脚本
```sql
-- 插入测试用户
INSERT INTO t_user (user_id, username, password, email, real_name, status) VALUES
(1, 'admin', '$2a$10$7JB720yubVSOfvVWbGReyO...', 'admin@mouse-monitor.com', '系统管理员', 1),
(2, 'researcher1', '$2a$10$7JB720yubVSOfvVWbGReyO...', 'researcher1@lab.com', '研究员张三', 1),
(3, 'annotator1', '$2a$10$7JB720yubVSOfvVWbGReyO...', 'annotator1@lab.com', '标注员李四', 1);

-- 插入测试设备
INSERT INTO t_device (device_id, device_name, device_type, location, status) VALUES
('dev_001', '采集端-实验室A-001', 'video_capture', '实验室A', 1),
('dev_002', '采集端-实验室A-002', 'video_capture', '实验室A', 1),
('dev_003', '采集端-实验室B-001', 'video_capture', '实验室B', 1);

-- 插入测试实验
INSERT INTO t_experiment (experiment_id, experiment_name, researcher_name, start_time, status) VALUES
('exp_001', '阿尔茨海默病药物疗效评价', '张三', '2024-12-01 09:00:00', 1),
('exp_002', '抗抑郁药物行为学研究', '李四', '2024-12-01 14:00:00', 1);

-- 插入测试鼠笼
INSERT INTO t_cage (cage_id, cage_name, experiment_id, device_id, capacity, current_count) VALUES
('cage_001', '实验笼A-001', 'exp_001', 'dev_001', 5, 5),
('cage_002', '实验笼A-002', 'exp_001', 'dev_002', 5, 4),
('cage_003', '实验笼B-001', 'exp_002', 'dev_003', 5, 3);

-- 插入测试小鼠
INSERT INTO t_mouse (mouse_id, mouse_name, cage_id, experiment_id, strain, gender, age, weight) VALUES
('mouse_001', 'C57BL/6-001', 'cage_001', 'exp_001', 'C57BL/6', 'M', 8, 25.5),
('mouse_002', 'C57BL/6-002', 'cage_001', 'exp_001', 'C57BL/6', 'F', 8, 22.3),
('mouse_003', 'C57BL/6-003', 'cage_001', 'exp_001', 'C57BL/6', 'M', 9, 26.1),
('mouse_004', 'C57BL/6-004', 'cage_001', 'exp_001', 'C57BL/6', 'F', 8, 21.8),
('mouse_005', 'C57BL/6-005', 'cage_001', 'exp_001', 'C57BL/6', 'M', 8, 24.9);

-- 插入测试二维码
INSERT INTO t_qr_code (qr_code_data, qr_code_type, target_id, image_url, status, description) VALUES
('MOUSE_mouse_001', 'mouse', 'mouse_001', '/qrcode/mouse_001.png', 1, 'C57BL/6-001小鼠标识码'),
('MOUSE_mouse_002', 'mouse', 'mouse_002', '/qrcode/mouse_002.png', 1, 'C57BL/6-002小鼠标识码'),
('MOUSE_mouse_003', 'mouse', 'mouse_003', '/qrcode/mouse_003.png', 1, 'C57BL/6-003小鼠标识码'),
('MOUSE_mouse_004', 'mouse', 'mouse_004', '/qrcode/mouse_004.png', 1, 'C57BL/6-004小鼠标识码'),
('MOUSE_mouse_005', 'mouse', 'mouse_005', '/qrcode/mouse_005.png', 1, 'C57BL/6-005小鼠标识码'),
('CAGE_cage_001', 'cage', 'cage_001', '/qrcode/cage_001.png', 1, '实验笼A-001标识码'),
('CAGE_cage_002', 'cage', 'cage_002', '/qrcode/cage_002.png', 1, '实验笼A-002标识码'),
('CAGE_cage_003', 'cage', 'cage_003', '/qrcode/cage_003.png', 1, '实验笼B-001标识码'),
('DEVICE_dev_001', 'device', 'dev_001', '/qrcode/dev_001.png', 1, '采集端-实验室A-001设备码'),
('DEVICE_dev_002', 'device', 'dev_002', '/qrcode/dev_002.png', 1, '采集端-实验室A-002设备码'),
('DEVICE_dev_003', 'device', 'dev_003', '/qrcode/dev_003.png', 1, '采集端-实验室B-001设备码');

-- 插入测试扫描记录
INSERT INTO t_qr_scan_log (qr_code_data, scan_user_id, device_info, location, scan_time, scan_result) VALUES
('MOUSE_mouse_001', '2', 'iPhone 12', '实验室A', '2024-12-01 10:30:00', 'success'),
('CAGE_cage_001', '2', 'iPhone 12', '实验室A', '2024-12-01 10:31:00', 'success'),
('DEVICE_dev_001', '1', 'Android Phone', '实验室A', '2024-12-01 11:00:00', 'success');}}}
```

#### 4.2 假数据生成服务
```java
@Service
public class MockDataService {
    
    @Autowired
    private IBehaviorDataService behaviorDataService;
    
    @Autowired
    private IQrCodeService qrCodeService;
    
    @PostConstruct
    public void initQrCodes() {
        // 初始化时生成所有二维码图片
        generateQrCodesForAllEntities();
    }
    
    @Scheduled(fixedRate = 5000) // 每5秒生成一次假数据
    public void generateMockBehaviorData() {
        List<String> mouseIds = Arrays.asList("mouse_001", "mouse_002", "mouse_003", "mouse_004", "mouse_005");
        List<String> behaviors = Arrays.asList("sleeping", "grooming", "scratching", "twitching", "defecating");
        
        BatchUploadRequest request = new BatchUploadRequest();
        request.setBatchId("batch_" + System.currentTimeMillis());
        request.setDeviceId("dev_001");
        request.setCageId("cage_001");
        request.setTimestamp(System.currentTimeMillis());
        
        List<BehaviorDataItem> dataList = new ArrayList<>();
        
        for (String mouseId : mouseIds) {
            BehaviorDataItem item = new BehaviorDataItem();
            item.setMouseId(mouseId);
            item.setTimestamp(System.currentTimeMillis());
            
            // 随机位置
            Position position = new Position();
            position.setX(Math.random() * 200);
            position.setY(Math.random() * 200);
            position.setConfidence(0.8 + Math.random() * 0.2);
            item.setPosition(position);
            
            // 随机行为
            Map<String, BehaviorDetection> behaviorMap = new HashMap<>();
            String activeBehavior = Math.random() < 0.3 ? behaviors.get((int)(Math.random() * behaviors.size())) : null;
            
            for (String behavior : behaviors) {
                BehaviorDetection detection = new BehaviorDetection();
                if (behavior.equals(activeBehavior)) {
                    detection.setDetected(true);
                    detection.setConfidence(0.7 + Math.random() * 0.25);
                    detection.setDuration((int)(Math.random() * 300) + 5);
                } else {
                    detection.setDetected(false);
                    detection.setConfidence(Math.random() * 0.3);
                }
                behaviorMap.put(behavior, detection);
            }
            item.setBehaviors(behaviorMap);
            
            // 运动数据
            MotionData motionData = new MotionData();
            motionData.setVelocity(activeBehavior != null && !"sleeping".equals(activeBehavior) ? Math.random() * 5 : Math.random() * 0.2);
            motionData.setAcceleration((Math.random() - 0.5) * 2);
            motionData.setDirection(Math.random() * 360);
            item.setMotionData(motionData);
            
            dataList.add(item);
        }
        
        request.setData(dataList);
        
        try {
            behaviorDataService.processBehaviorData(request);
            log.info("生成假数据成功，批次ID: {}", request.getBatchId());
        } catch (Exception e) {
            log.error("生成假数据失败", e);
        }
    }
}
```

## 第二个月开始：团队协作阶段

### 新成员加入计划

#### 第5周：团队扩展
- [ ] 新成员技术培训
- [ ] 代码规范和工作流培训
- [ ] 任务分工和协作机制建立
- [ ] Python采集端开发启动

#### 第6-8周：并行开发
- [ ] **后端开发**：数据分析模块、性能优化、二维码批量管理功能
- [ ] **前端开发**：移动端uni-app开发、二维码扫描和生成界面
- [ ] **Python端开发**：视频采集和行为识别、二维码识别集成
- [ ] **数据标注**：标注工具使用培训，标注任务开始

### 数据标注计划

#### 标注准备阶段（第5周）
- [ ] CVAT标注工具部署和配置
- [ ] 标注规范制定
- [ ] 标注人员培训
- [ ] 标注质量控制流程建立

#### 标注执行阶段（第6-12周）
- [ ] 收集小鼠视频数据
- [ ] 执行行为标注任务
- [ ] 质量检查和数据清洗
- [ ] 标注数据集构建

## 风险控制与应对策略

### 技术风险
1. **AI模型训练效果不佳**
   - 应对：准备多种算法方案，建立模型评估体系
   
2. **实时性能不满足要求**
   - 应对：分阶段性能测试，及时优化瓶颈
   
3. **数据质量问题**
   - 应对：建立数据验证机制，多重质量检查

### 进度风险
1. **开发进度延迟**
   - 应对：采用敏捷开发，定期评估和调整
   
2. **人员技能不匹配**
   - 应对：提前技能评估，制定培训计划

### 质量风险
1. **系统稳定性问题**
   - 应对：完善测试体系，建立监控机制
   
2. **用户体验不佳**
   - 应对：早期用户反馈，迭代优化

## 成功标准

### 第一个月目标
- [ ] Spring Boot后端核心功能完成度 ≥ 80%
- [ ] 二维码生成、识别、管理功能完整实现
- [ ] 假数据驱动的完整业务流程可演示
- [ ] API接口覆盖率 ≥ 90%
- [ ] 单元测试覆盖率 ≥ 70%

### 第二个月目标
- [ ] Python采集端基础功能完成（含二维码识别）
- [ ] 前端主要功能模块完成（含移动端二维码扫描）
- [ ] 数据标注流程建立
- [ ] 二维码与实体对象绑定关系完善
- [ ] 系统集成测试通过

### 整体项目目标
- [ ] 系统功能完整性 ≥ 95%
- [ ] 性能指标达到设计要求
- [ ] 用户满意度 ≥ 85%
- [ ] 代码质量评级 ≥ A级

通过这个详细的里程碑规划，我们可以确保项目按时、按质完成，同时为团队协作和后续扩展奠定坚实基础。